# Copy this file to .env and fill with your real database credentials
# Example PostgreSQL DATABASE_URL (adjust user, password, host, port, dbname):
# DATABASE_URL=postgresql://postgres:password@localhost:5432/nestn?schema=public

# If you use another DB (MySQL), provide the correct connection string.

# Optional: frontend origin used for CORS (default http://localhost:3000)
# FRONTEND_URL=http://localhost:3000
