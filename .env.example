# Environment Variables

## Backend (.env)

```
# Database
DATABASE_URL="postgresql://neondb_owner:npg_iXysvuJ5L0SD@ep-bold-feather-ahfibfb8-pooler.c-3.us-east-1.aws.neon.tech/ecom?sslmode=require&channel_binding=require"

# Server
PORT=3001
NODE_ENV=development

# JWT Secrets
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-in-production"

# CORS
FRONTEND_URL="http://localhost:3000"
```

## Frontend (.env.local)

```
NEXT_PUBLIC_API_URL=http://localhost:3001
```

### Production Notes

1. Ganti semua secret keys dengan random yang kuat
2. Set `NODE_ENV=production`
3. Set `FRONTEND_URL` ke domain frontend production
4. Gunakan PostgreSQL managed service (AWS RDS, Heroku Postgres, etc)
5. Set `DATABASE_URL` ke production database
6. Enable HTTPS
7. Set secure cookies di production
